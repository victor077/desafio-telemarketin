const useStyles = () => ({
  containerBody: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  containerGrid: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }
});

export default useStyles;
